spring:
  application:
    name: discovery

server: 
  port: 8088

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8088/eureka
    register-with-eureka: false
    fetch-registry: false

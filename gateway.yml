spring:
  application:
    name: login-service

server: 
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8088/eureka

zuul:
  prefix: /api
  routes: 
      login-service:
        path: /login-service/**
        serviceId:  LOGIN-SERVICE
                
